# We might have a conf and classes directory, append to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have a recipes directory, add to BBFILES
BBFILES += "${LAYERDIR}/recipes*/*/*.bb ${LAYERDIR}/recipes*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-quantum-safe"
BBFILE_PATTERN_meta-quantum-safe := "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-quantum-safe = "5"

# Core layer required; sample images/ptests rely on meta-openembedded (notably meta-python).
LAYERDEPENDS_meta-quantum-safe = "core openembedded-layer meta-python"
LAYERSERIES_COMPAT_meta-quantum-safe = "scarthgap kirkstone"

# Default to stable, pinned recipe; users can override to "git" if desired.
PREFERRED_VERSION_liboqs ?= "0.10.1"
